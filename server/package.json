{
  "name": "shopsquad",
  "version": "0.0.1",
  "description": "consumer loyality program",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon -w ./ -e js -x 'node index'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "six-hacaton-team",
  "license": "ISC",
  "dependencies": {
    "faker": "^4.1.0",
    "json-server": "^0.9.6"
  }
}
